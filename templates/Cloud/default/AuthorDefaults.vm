#if ($author.getFirstName())
	#set ($firstname = "$author.getFirstName()")
#else
	#set ($firstname = "SkyNet")
#end

#if ($author.getLastName())
	#set ($lastname = "$author.getLastName()")
#else
	#set ($lastname = "Alpha Build System")
#end

#if ($author.getEmail())
	#set ($email = "$author.getEmail()")
#else
	#set ($email = "robot@devnull.com")
#end